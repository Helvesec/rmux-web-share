{
  "name": "rmux-web-share",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build && node scripts/prepare-share-offline.mjs && node scripts/write-build-provenance.mjs && npm run share:budget",
    "preview": "astro preview --host 127.0.0.1",
    "test:e2e": "playwright test",
    "share:budget": "node scripts/check-share-bundle-budget.mjs"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.10.0",
    "@xterm/addon-web-links": "0.11.0",
    "@xterm/xterm": "5.5.0",
    "astro": "^6.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "wrangler": "^4.94.0"
  }
}
