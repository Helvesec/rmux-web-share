{
  "schema_version": 1,
  "artifact": "rmux-web-crypto WASM (Playwright-only, exposes ServerSession + mlKemEncapsulate)",
  "source": {
    "repository": "https://github.com/Helvesec/rmux",
    "crate": "crates/rmux-web-crypto",
    "feature_set": ["wasm-test"],
    "source_commit": "e2bce4c6885ef314c30f5fa79a521bfb6ef2a421",
    "source_commit_note": "The rmux commit these artifacts were built from. Re-stamped by scripts/build-web-crypto-wasm.sh on regeneration. This blob is test-only and never shipped to production."
  },
  "toolchain": {
    "rustc": "1.94.1",
    "rustc_note": "Exact rustc that reproduces these bytes. verify-wasm-from-source.mjs exports it as RUSTUP_TOOLCHAIN and applies --remap-path-prefix for source, Cargo, and rustup roots so local paths cannot change the emitted WASM.",
    "wasm_bindgen": "0.2.123",
    "wasm_bindgen_note": "These bytes are produced by wasm-pack 0.13.1's PINNED, DOWNLOADED prebuilt wasm-bindgen 0.2.123 — not a source-built cli. A `cargo install wasm-bindgen-cli --version 0.2.123` built from source emits the same .js glue but a DIFFERENT .wasm, so reproduction must use wasm-pack's prebuilt path (no conflicting wasm-bindgen on PATH). Building the cli reproducibly from source is tracked future hardening.",
    "wasm_opt": false,
    "target": "web"
  },
  "build_command": "RUSTFLAGS='--remap-path-prefix=<worktree>=/rmux-source --remap-path-prefix=<cargo-home>=/cargo --remap-path-prefix=<rustup-home>=/rustup' wasm-pack build crates/rmux-web-crypto --release --target web --out-name rmux_web_crypto_wasm -- --no-default-features --features wasm-test",
  "verification": "scripts/verify-wasm-provenance.mjs hash-pins this blob on every build. It is test-only and never shipped to production. Include it in the source-to-binary gate with RMUX_WASM_DIRS=wasm,wasm-test when a full test-blob audit is needed.",
  "artifacts": {
    "rmux_web_crypto_wasm_bg.wasm": "sha256:35917e36d1a0fcd257e0600491c1fc044247dbe1eaf61fd7de48aeda646cd611",
    "rmux_web_crypto_wasm.js": "sha256:61d0dee23b681fb05afc6f84550d2d3cf6fd74c276cc750b60aa0fa4938838a7"
  }
}
