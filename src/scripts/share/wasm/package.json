{
  "name": "rmux-web-crypto-wasm",
  "type": "module",
  "description": "WebAssembly bindings exposing the rmux web-share client record channel (sealed-channel + kind-byte framing) to the browser. X25519 and SHA-256 are done by the browser's WebCrypto; only the DH shared secret and PSK bytes are passed in.",
  "version": "0.4.0",
  "license": "MIT OR Apache-2.0",
  "files": [
    "rmux_web_crypto_wasm_bg.wasm",
    "rmux_web_crypto_wasm.js",
    "rmux_web_crypto_wasm.d.ts"
  ],
  "main": "rmux_web_crypto_wasm.js",
  "types": "rmux_web_crypto_wasm.d.ts",
  "sideEffects": [
    "./snippets/*"
  ]
}